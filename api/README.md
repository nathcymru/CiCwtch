# CiCwtch API

This directory contains the Cloudflare Workers API for the CiCwtch platform.

The API will provide REST endpoints used by the Flutter application.

Target platform:
Cloudflare Workers

Database:
Cloudflare D1 (SQLite)
